# Weather-App

React + TypeScript + Tailwind recreation of the original weather UI.

## Commands

- `npm install`
- `npm run dev`

This project keeps the same behavior and icons while migrating the UI to React.
